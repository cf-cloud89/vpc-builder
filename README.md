# vpc-builder
A Python CLI tool that builds a mini Virtual Private Cloud (VPC) on a single Linux host from first principles. It uses network namespaces, veth pairs, bridges, and iptables to simulate subnets (public/private), routing, NAT, and security groups.
